# The Discord bot is optional, and so is this.
#
#     pip install -r requirements-discord.txt
#
# Kept out of requirements.txt on purpose: the companion, the terminal
# interface and the local API all run without it, and someone who never
# wants a bot should not be made to install a gateway library to get them.
# discord_bot.py imports this lazily for the same reason — the pure logic
# and its tests run whether or not it is present.
discord.py>=2.3
